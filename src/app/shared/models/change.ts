export class Change {
  weightChange: number = 0;
  bmiChange: number = 0;

  // constructor(weightChange:number, bmiChange: number) {
  //   this.weightChange = weightChange;
  //   this.bmiChange = bmiChange;
  // }
}
